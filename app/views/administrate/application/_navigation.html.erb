<nav class="navigation">
  <%= link_to(t("administrate.navigation.back_to_app"), root_url, class: "button button--alt button--nav") if defined?(root_url) %>

  <% Administrate::Namespace.new(namespace).resources_with_index_route.each do |resource| %>
    <% next if ["bureau_projects", "bureau_categories"].include?(resource.to_s) %>
    <%= link_to(
      display_resource_name(resource),
      resource_index_route(resource),
      class: "navigation__link navigation__link--#{nav_link_state(resource)}"
    ) if accessible_action?(model_from_resource(resource), :index) %>
  <% end %>

  <%= link_to "Bureau", admin_bureau_projects_path, class: "navigation__link" if accessible_action?(BureauProject, :index) %>
  <%= link_to "Catégories Bureau", admin_bureau_categories_path, class: "navigation__link" if accessible_action?(BureauCategory, :index) %>
</nav>
